表單用 material 的寫法
html
<div class="mat-elevation-z8">
  <table mat-table [dataSource]="dataSource">

    <ng-container matColumnDef="formID">
      <th mat-header-cell *matHeaderCellDef> 序號 </th>
      <td mat-cell *matCellDef="let element"> {{element.formID}} </td>
    </ng-container>

    <ng-container matColumnDef="formTitle">
      <th mat-header-cell *matHeaderCellDef> 標題 </th>
      <td mat-cell *matCellDef="let element"> {{element.formTitle}} </td>
    </ng-container>

    <ng-container matColumnDef="formDescription">
      <th mat-header-cell *matHeaderCellDef> 說明 </th>
      <td mat-cell *matCellDef="let element"> {{element.formDescription}} </td>
    </ng-container>

    <ng-container matColumnDef="formIsOpen">
      <th mat-header-cell *matHeaderCellDef> 狀態 </th>
      <td mat-cell *matCellDef="let element"> {{element.formIsOpen}} </td>
    </ng-container>

    <ng-container matColumnDef="formSDate">
      <th mat-header-cell *matHeaderCellDef> 開始時間 </th>
      <td mat-cell *matCellDef="let element"> {{element.formSDate}} </td>
    </ng-container>

    <ng-container matColumnDef="formEDate">
      <th mat-header-cell *matHeaderCellDef> 結束時間 </th>
      <td mat-cell *matCellDef="let element"> {{element.formEDate}} </td>
    </ng-container>

    <ng-container matColumnDef="formType">
      <th mat-header-cell *matHeaderCellDef> 結果 </th>
      <td mat-cell *matCellDef="let element"> {{element.formType}} </td>
    </ng-container>

    <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
    <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
  </table>

  <mat-paginator [pageSizeOptions]="[5, 10, 20]" showFirstLastButtons aria-label="Select page of periodic elements">
  </mat-paginator>
</div>

ts
import { ViewChild } from '@angular/core';
import { MatPaginator, MatPaginatorModule } from '@angular/material/paginator';
import { MatTableDataSource, MatTableModule } from '@angular/material/table';

imports: [MatTableModule, MatPaginatorModule]

// 表單資料
formsData: any[] = [];

// Material Table
displayedColumns: string[] = ['formID', 'formTitle', 'formDescription', 'formIsOpen', 'formSDate', 'formEDate', 'formType'];
dataSource!: MatTableDataSource<any>;

@ViewChild(MatPaginator) paginator!: MatPaginator;

constructor(private formsService: FormsService) { }

ngOnInit(): void {
  // 初始化資料
  this.formsData = this.formsService.forms;
  this.dataSource = new MatTableDataSource<any>(this.formsData);
}

ngAfterViewInit(): void {
  // 綁定分頁器
  this.dataSource.paginator = this.paginator;
}
